<UserControl
    x:Class="LayuiComponentExample.Views.Pagination"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Lay="clr-namespace:LayuiTemplate.Control;assembly=LayuiTemplate"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    prism:ViewModelLocator.AutoWireViewModel="True">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Lay:LayDataGrid IsActive="{Binding IsActive}" ItemsSource="{Binding ListData}">
            <Lay:LayDataGrid.Columns>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Name, Mode=OneWay}"
                    Header="名称" />
            </Lay:LayDataGrid.Columns>
        </Lay:LayDataGrid>
        <Lay:LayPagination
            Grid.Row="1"
            Height="30"
            Margin="0,5"
            FontSize="12"
            Limit="10"
            PageCount="100" />
    </Grid>
</UserControl>
