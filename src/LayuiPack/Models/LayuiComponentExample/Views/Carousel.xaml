<UserControl
    x:Class="LayuiComponentExample.Views.Carousel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:lay="clr-namespace:LayuiTemplate.Control;assembly=LayuiTemplate"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True">
    <lay:LayCarousel Margin="0,10" Style="{DynamicResource LayCarouselStyle}">
        <Button />
        <!--<lay:LayCarousel.ItemTemplate>
            <DataTemplate>
                <Button>
                    <Image
                        Width="{Binding RelativeSource={RelativeSource AncestorType=lay:LayCarousel}, Path=ActualWidth}"
                        Height="{Binding RelativeSource={RelativeSource AncestorType=lay:LayCarousel}, Path=ActualHeight}"
                        Source="{Binding .}"
                        Stretch="Fill" />
                </Button>
            </DataTemplate>
        </lay:LayCarousel.ItemTemplate>-->
    </lay:LayCarousel>
</UserControl>
